import { test } from '@playwright/test';

test.describe('Bootstrap placeholder', () => {
  test('pending implementation', async () => {
    test.info().annotations.push({ type: 'todo', description: 'Scénarios E2E à définir à l’étape 5.' });
  });
});

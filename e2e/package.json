{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pretest": "playwright install --with-deps chromium",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1"
  }
}
